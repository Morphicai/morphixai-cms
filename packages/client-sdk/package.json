{
  "name": "@optimus/client-sdk",
  "version": "1.0.0",
  "description": "Optimus C 端 SDK：登录态/HTTP/存储/动态内容/数据集合，任意 web 子应用可用",
  "author": "Optimus Team <team@example.com>",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://registry.npmmirror.com/"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "test": "echo \"No tests in client-sdk package\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^5.0.0"
  }
}
