{
  "name": "@optimus/permission",
  "version": "1.0.0",
  "description": "Optimus 权限判定核心：三段式权限码的解析、通配匹配与判定，零运行时依赖",
  "author": "Optimus Team <team@example.com>",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://registry.npmmirror.com/"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "test": "tsc && node --test \"dist/__tests__/*.test.js\""
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
