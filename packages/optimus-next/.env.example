# optimus-next 环境变量样板
# 复制为 .env.local 按需修改；全部有代码内默认值，本地零配置可跑

# 业务后台地址（Next 代理转发目标，服务端变量，不暴露给浏览器）
# 默认 http://localhost:8084/api；Docker 单容器部署时同样是这个值
OPTIMUS_API_URL=http://localhost:8084/api

# 浏览器侧 API 基地址。默认走同源 /api 代理（推荐，httpOnly cookie 依赖同源）。
# 只有在需要绕过代理直连后台时才设置它——那会破坏 cookie 认证，想清楚再用
# NEXT_PUBLIC_API_URL=/api
